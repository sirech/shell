Host *
     TCPKeepAlive yes
     ServerAliveInterval 15
     ServerAliveCountMax 6
     Compression yes

     # Reuse connections to the same host
     ControlMaster auto
     ControlPath /tmp/ssh_mux_%h_%p_%r

     GSSAPIAuthentication no

     IgnoreUnknown UseKeychain
     UseKeychain yes

Host erza
     HostName 45.32.187.162
     User root
